#ifndef SPOONY_OPTIMIZER_HH
#define SPOONY_OPTIMIZER_HH

#include <memory>

#include "duration.hh"
#include "encounter.hh"

class Engine;
class Options;

/*
void optimize_ils(std::size_t start_index, Milliframes * best_frames, double * best_score, const Options & options, const std::shared_ptr<Variables> & variables, Engine * engine, const Engine & base_engine, const std::string & output_filename);
void optimize_local(std::size_t start_index, Milliframes * best_frames, double * best_score, const Options & options, const std::shared_ptr<Variables> & variables, Engine * engine, const Engine & base_engine, const std::string & output_filename, bool final_newline);
void optimize_local_pair(std::size_t start_index, Milliframes * best_frames, double * best_score, const Options & options, const std::shared_ptr<Variables> & variables, Engine * engine, const Engine & base_engine, const std::string & output_filename, bool final_newline);
void optimize_sequential(std::size_t start_index, Milliframes * best_frames, double * best_score, const Options & options, const std::shared_ptr<Variables> & variables, Engine * engine, const Engine & base_engine, const std::string & output_filename);
*/

#endif // SPOONY_OPTIMIZER_HH
